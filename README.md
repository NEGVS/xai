# xai

只要是安装了新的包，都看先查看版本，搜索当前版本是否是最新的或是你需要的。要不然往往折腾半天发现是版本问题。

xai
asdkjfa
asdklfj 

## 1-判断类型
print(type(data))
## 2-解析jsonStr
import json
json_data = json.loads(str(data))

# 1-按照包步骤
升级pip
pip install --upgrade pip

如果仍然报错，可能是依赖问题。尝试创建一个干净的虚拟环境并重新安装：
bashpython -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
---
1-安装
pip install langchain-openai
确认你使用的 Python 环境已安装 langchain_openai
pip show langchain-openai

=====
from ipywidgets import interact
这是 Python 中用于创建交互式控件的库，非常适合把刚才的 “钱的重量 vs 价值” 计算做成可交互的小工具，让你能自己调整参数（比如不同面额、不同重量）来计算结果。
